on:
  pull_request:

name: Docker

jobs:
  build_docker_image:
    name: Build Docker image
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Build Docker image
        run: docker build --no-cache -t dbrgn/flugbuech:latest .
